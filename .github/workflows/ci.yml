name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Typecheck
        run: npx astro check

      - name: Build
        run: npm run build
        env:
          # Dummy values so the SSR build can resolve import.meta.env types and
          # run prerendering — no live services are contacted at build time.
          PUBLIC_POSTHOG_KEY: phc_ci_placeholder
          PUBLIC_POSTHOG_HOST: https://us.i.posthog.com
          PUBLIC_SITE_URL: https://tryfxreplay.com
          DATABASE_URL: postgres://ci:ci@localhost:5432/ci
          SUPABASE_URL: https://ci.supabase.co
          SUPABASE_ANON_KEY: ci
          SUPABASE_SERVICE_ROLE_KEY: ci
